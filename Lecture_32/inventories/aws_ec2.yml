plugin: amazon.aws.aws_ec2
regions: [us-east-1]

hostnames:
  - tag:Name

compose:
  ansible_host: public_ip_address
  ansible_user: "'ubuntu'" #оце просто всратий запис, але без нього не працює
  ansible_ssh_private_key_file: "'~/Documents/hw32.pem'"